.DS_Store

# Camera masters for the Headroom videos (~4.4GB). These are the originals the
# HLS renditions are transcoded from; the renditions are published from the
# separate headroom-video repo, so neither belongs in this repo's history.
headroom/Videos/
